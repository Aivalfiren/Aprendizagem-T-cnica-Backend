// destruturar uma array

const pessoas = ['flavia', 'livia', 'ana']

const [a, b, c] = pessoas   // posso dar qualquer nome ou letra para variavel de destruturação pois não tem propriedade na array.

console.log(a, b, c)